meta {
  name: post user
  type: http
  seq: 2
}

post {
  url: {{base_url}}/users
  body: json
  auth: none
}

body:json {
  {
    "userId":"{{newuuid}}",
    "name":"nameeo"
  }
}

script:pre-request {
  const {v4} = require('uuid');
  bru.setEnvVar("newuuid", v4());
}
