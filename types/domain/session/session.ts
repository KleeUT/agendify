export interface SessionModel {
  conferenceId: string;
  sessionId: string;
  speakerIds: Array<string>;
  title: string;
  abstract: string;
  tags: Array<string>;
}
export interface SessionDetails {
  conferenceId: string;
  sessionId: string;
  speakerIds: Array<string>;
  title: string;
  abstract: string;
  tags: Array<string>;
}
